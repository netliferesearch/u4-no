{
  "environments": {
    "test": {
      "scripts": {
        "test-setup":
          "cd frontend && npm install && npm run next build && npm run sass-build && npm start",
        "test": "cd frontend && npm test"
      }
    }
  }
}
